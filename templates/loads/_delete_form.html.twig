<form method="post" action="{{ path('app_loads_delete', {'id': load.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ load.id) }}">
    <button class="btn">Delete</button>
</form>
